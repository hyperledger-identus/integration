# Identus Integration Suite - Environment Variables
# 
# Copy this file to .env and configure the values for your environment.
# Required variables must be set, optional variables can be omitted.

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# GitHub Personal Access Token
# Required for: All operations
# Permissions needed: repo (for private repos), read:packages (if using GitHub Packages)
# Generate at: https://github.com/settings/tokens
GH_TOKEN=ghp_your_github_token_here

# =============================================================================
# OPTIONAL VARIABLES - Notifications
# =============================================================================

# Slack Webhook URL for failure notifications
# Required for: Slack notifications on test failures
SLACK_WEBHOOK=your-slack-webhook-url-here

# =============================================================================
# OPTIONAL VARIABLES - Cloud Services
# =============================================================================

# Cloud Service Configuration (required for cloud:setup/check commands)
CLOUD_SERVICE_URL=https://your-cloud-service.example.com
CLOUD_SERVICE_PROJECT=your-project-id
CLOUD_SERVICE_TOKEN=your-cloud-service-token

# =============================================================================
# OPTIONAL VARIABLES - Service URLs (for local development)
# =============================================================================

# Agent URL for local development/testing
AGENT_URL=http://localhost:8085

# Mediator OOB URL for local development/testing
MEDIATOR_OOB_URL=http://localhost:8080/invitationOOB

# =============================================================================
# OPTIONAL VARIABLES - Configuration
# =============================================================================

# Enable debug mode for detailed logging (set to true, 1, or any non-empty string)
DEBUG=false

# CI environment flag (automatically set in GitHub Actions)
CI=

# External base URL for report links (defaults to GitHub Pages URL)
EXTERNAL_BASE_URL=https://hyperledger-identus.github.io/integration/

# =============================================================================
# WORKFLOW-SPECIFIC VARIABLES (set by GitHub Actions, not manually)
# =============================================================================

# Component name: cloud-agent, mediator, sdk-ts, sdk-kmp, sdk-swift, weekly, release, manual
COMPONENT=

# Version string: Semantic version (e.g., 1.0.0) or commit SHA for libraries
VERSION=

# GitHub Actions run ID (used for artifact download and workflow linking)
RUN_ID=

# Base64-encoded environment configuration (contains full environment config object)
ENV=
