# Required Environment Variables
ENV=base64-encoded-environment-config
GH_TOKEN=your-github-token-here

# Optional Environment Variables
SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
DEBUG=true
CI=true

# Component-specific Variables
COMPONENT=cloud-agent
VERSION=main
RUN_ID=12345

# Service URLs
MEDIATOR_OOB_URL=https://mediator.example.com/invitationOOB
AGENT_URL=https://agent.example.com/

# Notes:
# - ENV should be a base64-encoded JSON string with environment configuration
# - GH_TOKEN needs repo access for fetching release tags and commits
# - SLACK_WEBHOOK is optional - if not set, Slack notifications will be skipped
# - Set DEBUG=true to see detailed command output
# - Set CI=true to enable CI mode (disables spinners, shows output)