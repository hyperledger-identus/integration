# SonarCloud configuration for automatic analysis.
#
# Excludes CI-only infrastructure (Docker compose, DB init scripts with
# intentionally hardcoded demo credentials) and static Allure report
# placeholder HTML files (auto-generated, not application code) from
# analysis.  The TypeScript application in src/ and tests/ is fully analysed.

sonar.exclusions=infra/**,initial-pages/reports/**